<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.aprille.tryfixactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Blois : Savoir Écouter"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".PlanActivity"
            android:label="Plan de Blois">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AboutActivity"
            android:label="@string/title_activity_about"
            android:parentActivityName=".PlanActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="net.aprille.tryfixactivity.PlanActivity" />
        </activity>
        <activity
            android:name=".ThanksActivity"
            android:label="@string/title_activity_thanks"
            android:parentActivityName=".PlanActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="net.aprille.tryfixactivity.PlanActivity" />
        </activity>
        <activity
            android:name=".PeopleActivity"
            android:label="@string/title_activity_people"
            android:parentActivityName=".PlanActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="net.aprille.tryfixactivity.PlanActivity" />
        </activity>
        <activity
            android:name=".LegendeActivity"
            android:label="@string/title_activity_legende"
            android:parentActivityName=".PlanActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="net.aprille.tryfixactivity.PlanActivity" />
        </activity>
        <activity
            android:name=".AppActivity"
            android:label="@string/title_activity_app"
            android:parentActivityName=".PlanActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="net.aprille.tryfixactivity.PlanActivity" />
        </activity>
    </application>

</manifest>